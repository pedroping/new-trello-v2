import { CardMoveStopDirective } from './card-move-stop.directive';

describe('Directive: CardMoveStop', () => {
  it('should create an instance', () => {
    const directive = new CardMoveStopDirective();
    expect(directive).toBeTruthy();
  });
});
