import { CardMoveStartDirective } from './card-move-start.directive';

describe('Directive: CardMoveStart', () => {
  it('should create an instance', () => {
    const directive = new CardMoveStartDirective();
    expect(directive).toBeTruthy();
  });
});
