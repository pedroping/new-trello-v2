import { ListMoveStartDirective } from './list-move-start.directive';

describe('Directive: ListMoveStart', () => {
  it('should create an instance', () => {
    const directive = new ListMoveStartDirective();
    expect(directive).toBeTruthy();
  });
});
