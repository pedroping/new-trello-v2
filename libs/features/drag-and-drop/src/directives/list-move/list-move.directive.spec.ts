/* tslint:disable:no-unused-variable */

import { ListMoveDirective } from './list-move.directive';

describe('Directive: ListMove', () => {
  it('should create an instance', () => {
    const directive = new ListMoveDirective();
    expect(directive).toBeTruthy();
  });
});
