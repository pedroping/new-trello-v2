import { ListMoveStopDirective } from './list-move-stop.directive';

describe('Directive: ListMoveStop', () => {
  it('should create an instance', () => {
    const directive = new ListMoveStopDirective();
    expect(directive).toBeTruthy();
  });
});
